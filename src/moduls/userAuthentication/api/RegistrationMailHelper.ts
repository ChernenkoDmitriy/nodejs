import { ISendRegistrationMail } from "../useCases/_ports/ISendRegistrationMail";

export class RegistrationMailHelper implements ISendRegistrationMail {

    sendActivationMail = async (email: string, link: string) => {
        try {

        } catch (error) {

        }
    }

}
